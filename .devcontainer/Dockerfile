# Hériter de l'image de base devcontainers
FROM mcr.microsoft.com/devcontainers/universal:2

# Le Dockerfile de base est utilisé; les features (Java, Node, Docker) seront installées
# après ce Dockerfile par le système devcontainer.
# Les symlinks pour Java seront créés par setup.sh après l'installation.

